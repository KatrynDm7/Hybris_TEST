#include "internal/types.h"int main (){	int     	i 	= 0;	Table   	T 	= table::alloc ();	int     	cnt	= table::count (0);		if (!T) return;	while (i < cnt)	{		table::get (T, 0, i);		table::broaden_to_frame (T, -1);		i++;	}	table::release (T);		return 0;}