# -----------------------------------------------------------------------
# [y] hybris Platform 
# 
# Copyright (c) 2000-2013 hybris AG
# All rights reserved.
#  
# This software is the confidential and proprietary information of hybris 
# ("Confidential Information"). You shall not disclose such Confidential
# Information and shall use it only in accordance with the terms of the
# license agreement you entered into with hybris.
#
#  
# -----------------------------------------------------------------------

# you can put key/value pairs here.
# Use Config.getParameter(..) to retrieve the values during runtime.

sapproductconfigservices.key=value

# Specifies the location of the spring context file putted automatically to the global platform application context.
sapproductconfigservices.application-context=sapproductconfigservices-spring.xml

# Specify how many concurrent locks should be stored in parallel (more than 1024 could cause a performance issue)
sapproductconfigservices.maxLocksPerMap=1024 

# Specify how many configurations should be cached in parallel overall sessions to reduce read-access to the underlying Config-API.
# A configuration can always be re-read from the underlying API if it was removed from the cache.
sapproductconfigservices.maxCachedConfigsInSession=1024 

# Specifies which boolean field name in the ProductModel defines, if a product is configurable (e.g. when using the sapmodel extension, the value should be 'sapConfigurable')
sapproductconfigservices.configurable_fieldname=sapConfigurable

# Specifies which field name in the AbstractOrderEntry (parent for both, cart and oder) should be used to store the external configuration
# (e.g. when using the sapmodel extension, the value should be 'externalConfiguration')
sapproductconfigservices.externalConfiguration_fieldname=externalConfiguration